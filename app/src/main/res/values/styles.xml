<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="GeoPlus.Theme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_principal</item>
        <item name="colorSecondary">@color/light_secondary</item>
        <item name="colorOnPrimary">@color/light_secondary</item>
        <item name="android:textColorPrimary">@color/light_text</item>
        <item name="android:textColor">@color/light_text</item>
        <item name="android:background">@color/light_principal</item>
    </style>

    <style name="GeoPlus.Theme.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/dark_principal</item>
        <item name="colorSecondary">@color/dark_secondary</item>
        <item name="colorOnPrimary">@color/dark_secondary</item>
        <item name="android:textColorPrimary">@color/dark_text</item>
        <item name="android:textColor">@color/dark_text</item>
        <item name="android:background">@color/dark_principal</item>
    </style>

    <!-- Estilo para el botón -->
    <style name="ButtonStyle">
        <item name="android:backgroundTint">@color/principal_color</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:strokeColor">@color/secondary_color</item>
        <item name="android:strokeWidth">3</item>
    </style>

    <!-- Estilo para el texto -->
    <style name="TextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <!-- Estilo para el LinearLayout (si se quiere aplicar un fondo o padding) -->
    <style name="LinearLayoutStyle">
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <!-- Estilo para los ImageButton -->
    <style name="ImageButtonStyle">
        <item name="android:layout_marginEnd">25dp</item>
        <item name="android:src">@android:drawable/ic_menu_preferences</item>
    </style>

    <!-- Estilo para el ImageView -->
    <style name="ImageViewStyle">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">200dp</item>
        <item name="android:src">@drawable/geoplus_not_background</item>
    </style>

    <style name="Theme.GeoPlus" parent="GeoPlus.Theme" />
</resources>